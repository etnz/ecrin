version: '3.8'

services:
  verifier:
    image: traefik/whoami
    container_name: ecrin-verifier
    ports:
      - "8080:80"
    environment:
      - SUPER_SECRET=${hello:-SecretUnavailable}
    restart: always